/**
 * add webhook payload builders
 * Sharpy SDK — utility module
 */

export function webhooksupportHelper(input: unknown): unknown {
  // Implementation: add webhook payload builders
  return input;
}

export { webhooksupportHelper as default };
