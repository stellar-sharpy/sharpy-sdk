/**
 * add contract interface types
 * Sharpy SDK — utility module
 */

export function contractinterfacesHelper(input: unknown): unknown {
  // Implementation: add contract interface types
  return input;
}

export { contractinterfacesHelper as default };
