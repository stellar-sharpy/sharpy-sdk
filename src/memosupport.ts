/**
 * add transaction memo support
 * Sharpy SDK — utility module
 */

export function memosupportHelper(input: unknown): unknown {
  // Implementation: add transaction memo support
  return input;
}

export { memosupportHelper as default };
