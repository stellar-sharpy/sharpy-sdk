/**
 * add mock contract responses
 * Sharpy SDK — utility module
 */

export function mockcontractsHelper(input: unknown): unknown {
  // Implementation: add mock contract responses
  return input;
}

export { mockcontractsHelper as default };
