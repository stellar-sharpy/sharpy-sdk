/**
 * add fee estimation helpers
 * Sharpy SDK — utility module
 */

export function estimatefeesHelper(input: unknown): unknown {
  // Implementation: add fee estimation helpers
  return input;
}

export { estimatefeesHelper as default };
