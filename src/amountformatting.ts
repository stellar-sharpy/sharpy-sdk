/**
 * add amount formatting helpers
 * Sharpy SDK — utility module
 */

export function amountformattingHelper(input: unknown): unknown {
  // Implementation: add amount formatting helpers
  return input;
}

export { amountformattingHelper as default };
